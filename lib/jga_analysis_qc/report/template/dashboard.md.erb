## coverage statistics (Picard CollectWgsMetrics)

<% coverage_stats_html.each do |chr_region, htmls_of_chr_region| -%>
### <%= chr_region.desc %>
<% htmls_of_chr_region.each do |col, html| -%>
#### <%= col.label %>
<%= html %>
<% end -%>
<% end -%>

## mean coverage plot

### autosome-PAR mean coverage

![autosome-PAR mean coverage](<%= autosome_PAR_mean_coverage_plot_path -%>)

### chrX-nonPAR and chrY-nonPAR normalized mean coverage

![chrXY-nonPAR mean coverage](<%= chrXY_nonPAR_normalized_mean_coverage_plot_path -%>)

## ts/tv (bcftools stats)

<% ts_tv_ratio_html.each do |chr_region, html| -%>
### <%= chr_region.desc %>
<%= html %>
<% end -%>

## coverage statistics (Picard CollectWgsMetrics)

<% coverage_stats_html.each do |chr_region, htmls_of_chr_region| -%>
### <%= chr_region.desc %>
<% htmls_of_chr_region.each do |col, html| -%>
#### <%= col.label %>
<%= html %>
<% end -%>
<% end -%>

## mean coverage plot

### autosome_PAR mean coverage

![autosome_PAR mean coverage](<%= autosome_mean_coverage_plot_path -%>)

### chrX_nonPAR and chrY_nonPAR normalized mean coverage

![chrXY_nonPAR mean coverage](<%= chrXY_normalized_mean_coverage_plot_path -%>)

## ts/tv (bcftools stats)

<% ts_tv_ratio_html.each do |chr_region, html| -%>
### <%= chr_region.desc %>
<%= html %>
<% end -%>

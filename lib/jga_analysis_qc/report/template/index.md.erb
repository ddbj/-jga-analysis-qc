# Per-sample haplotye calling report

directory: `<%= result_dir.to_s %>`

* <%= Render.markdown_link_text('Progress and sample reports', progress_html_paths.first.basename&.to_s) %>
* <%= Render.markdown_link_text('Dashboard', dashboard_html_path.basename.to_s) %>

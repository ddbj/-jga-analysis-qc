# Progress

page: <%= paging.current_page %> / <%= paging.total_page %>

<%= navigation_markdown_text(PROGRESS_PREFIX, paging) %>

<%= table.markdown_text %>

<%= navigation_markdown_text(PROGRESS_PREFIX, paging) %>

# Progress

page: <%= paging.current_page %> / <%= paging.total_page %>

<%= navigation_markdown_text(TEMPLATE_PREFIX, paging) %>

<%= table.markdown_text %>

<%= navigation_markdown_text(TEMPLATE_PREFIX, paging) %>
